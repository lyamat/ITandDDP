export class Image {
    /**
     * 
     * @param {number[]} dimensions 
     * @param {string} url 
     */
    constructor (dimensions, url) {
        this.dimensions = dimensions;
        this.url = url;
    }
}